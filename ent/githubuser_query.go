// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"slackbot/ent/githubuser"
	"slackbot/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// GithubUserQuery is the builder for querying GithubUser entities.
type GithubUserQuery struct {
	config
	ctx        *QueryContext
	order      []githubuser.OrderOption
	inters     []Interceptor
	predicates []predicate.GithubUser
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GithubUserQuery builder.
func (guq *GithubUserQuery) Where(ps ...predicate.GithubUser) *GithubUserQuery {
	guq.predicates = append(guq.predicates, ps...)
	return guq
}

// Limit the number of records to be returned by this query.
func (guq *GithubUserQuery) Limit(limit int) *GithubUserQuery {
	guq.ctx.Limit = &limit
	return guq
}

// Offset to start from.
func (guq *GithubUserQuery) Offset(offset int) *GithubUserQuery {
	guq.ctx.Offset = &offset
	return guq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (guq *GithubUserQuery) Unique(unique bool) *GithubUserQuery {
	guq.ctx.Unique = &unique
	return guq
}

// Order specifies how the records should be ordered.
func (guq *GithubUserQuery) Order(o ...githubuser.OrderOption) *GithubUserQuery {
	guq.order = append(guq.order, o...)
	return guq
}

// First returns the first GithubUser entity from the query.
// Returns a *NotFoundError when no GithubUser was found.
func (guq *GithubUserQuery) First(ctx context.Context) (*GithubUser, error) {
	nodes, err := guq.Limit(1).All(setContextOp(ctx, guq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{githubuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (guq *GithubUserQuery) FirstX(ctx context.Context) *GithubUser {
	node, err := guq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GithubUser ID from the query.
// Returns a *NotFoundError when no GithubUser ID was found.
func (guq *GithubUserQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = guq.Limit(1).IDs(setContextOp(ctx, guq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{githubuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (guq *GithubUserQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := guq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GithubUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GithubUser entity is found.
// Returns a *NotFoundError when no GithubUser entities are found.
func (guq *GithubUserQuery) Only(ctx context.Context) (*GithubUser, error) {
	nodes, err := guq.Limit(2).All(setContextOp(ctx, guq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{githubuser.Label}
	default:
		return nil, &NotSingularError{githubuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (guq *GithubUserQuery) OnlyX(ctx context.Context) *GithubUser {
	node, err := guq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GithubUser ID in the query.
// Returns a *NotSingularError when more than one GithubUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (guq *GithubUserQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = guq.Limit(2).IDs(setContextOp(ctx, guq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{githubuser.Label}
	default:
		err = &NotSingularError{githubuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (guq *GithubUserQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := guq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GithubUsers.
func (guq *GithubUserQuery) All(ctx context.Context) ([]*GithubUser, error) {
	ctx = setContextOp(ctx, guq.ctx, "All")
	if err := guq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GithubUser, *GithubUserQuery]()
	return withInterceptors[[]*GithubUser](ctx, guq, qr, guq.inters)
}

// AllX is like All, but panics if an error occurs.
func (guq *GithubUserQuery) AllX(ctx context.Context) []*GithubUser {
	nodes, err := guq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GithubUser IDs.
func (guq *GithubUserQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if guq.ctx.Unique == nil && guq.path != nil {
		guq.Unique(true)
	}
	ctx = setContextOp(ctx, guq.ctx, "IDs")
	if err = guq.Select(githubuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (guq *GithubUserQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := guq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (guq *GithubUserQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, guq.ctx, "Count")
	if err := guq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, guq, querierCount[*GithubUserQuery](), guq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (guq *GithubUserQuery) CountX(ctx context.Context) int {
	count, err := guq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (guq *GithubUserQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, guq.ctx, "Exist")
	switch _, err := guq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (guq *GithubUserQuery) ExistX(ctx context.Context) bool {
	exist, err := guq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GithubUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (guq *GithubUserQuery) Clone() *GithubUserQuery {
	if guq == nil {
		return nil
	}
	return &GithubUserQuery{
		config:     guq.config,
		ctx:        guq.ctx.Clone(),
		order:      append([]githubuser.OrderOption{}, guq.order...),
		inters:     append([]Interceptor{}, guq.inters...),
		predicates: append([]predicate.GithubUser{}, guq.predicates...),
		// clone intermediate query.
		sql:  guq.sql.Clone(),
		path: guq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GhUsername string `json:"gh_username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GithubUser.Query().
//		GroupBy(githubuser.FieldGhUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (guq *GithubUserQuery) GroupBy(field string, fields ...string) *GithubUserGroupBy {
	guq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GithubUserGroupBy{build: guq}
	grbuild.flds = &guq.ctx.Fields
	grbuild.label = githubuser.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GhUsername string `json:"gh_username,omitempty"`
//	}
//
//	client.GithubUser.Query().
//		Select(githubuser.FieldGhUsername).
//		Scan(ctx, &v)
func (guq *GithubUserQuery) Select(fields ...string) *GithubUserSelect {
	guq.ctx.Fields = append(guq.ctx.Fields, fields...)
	sbuild := &GithubUserSelect{GithubUserQuery: guq}
	sbuild.label = githubuser.Label
	sbuild.flds, sbuild.scan = &guq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GithubUserSelect configured with the given aggregations.
func (guq *GithubUserQuery) Aggregate(fns ...AggregateFunc) *GithubUserSelect {
	return guq.Select().Aggregate(fns...)
}

func (guq *GithubUserQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range guq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, guq); err != nil {
				return err
			}
		}
	}
	for _, f := range guq.ctx.Fields {
		if !githubuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if guq.path != nil {
		prev, err := guq.path(ctx)
		if err != nil {
			return err
		}
		guq.sql = prev
	}
	return nil
}

func (guq *GithubUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GithubUser, error) {
	var (
		nodes = []*GithubUser{}
		_spec = guq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GithubUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GithubUser{config: guq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, guq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (guq *GithubUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := guq.querySpec()
	_spec.Node.Columns = guq.ctx.Fields
	if len(guq.ctx.Fields) > 0 {
		_spec.Unique = guq.ctx.Unique != nil && *guq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, guq.driver, _spec)
}

func (guq *GithubUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(githubuser.Table, githubuser.Columns, sqlgraph.NewFieldSpec(githubuser.FieldID, field.TypeUUID))
	_spec.From = guq.sql
	if unique := guq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if guq.path != nil {
		_spec.Unique = true
	}
	if fields := guq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, githubuser.FieldID)
		for i := range fields {
			if fields[i] != githubuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := guq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := guq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := guq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := guq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (guq *GithubUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(guq.driver.Dialect())
	t1 := builder.Table(githubuser.Table)
	columns := guq.ctx.Fields
	if len(columns) == 0 {
		columns = githubuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if guq.sql != nil {
		selector = guq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if guq.ctx.Unique != nil && *guq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range guq.predicates {
		p(selector)
	}
	for _, p := range guq.order {
		p(selector)
	}
	if offset := guq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := guq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GithubUserGroupBy is the group-by builder for GithubUser entities.
type GithubUserGroupBy struct {
	selector
	build *GithubUserQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gugb *GithubUserGroupBy) Aggregate(fns ...AggregateFunc) *GithubUserGroupBy {
	gugb.fns = append(gugb.fns, fns...)
	return gugb
}

// Scan applies the selector query and scans the result into the given value.
func (gugb *GithubUserGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gugb.build.ctx, "GroupBy")
	if err := gugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GithubUserQuery, *GithubUserGroupBy](ctx, gugb.build, gugb, gugb.build.inters, v)
}

func (gugb *GithubUserGroupBy) sqlScan(ctx context.Context, root *GithubUserQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gugb.fns))
	for _, fn := range gugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gugb.flds)+len(gugb.fns))
		for _, f := range *gugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GithubUserSelect is the builder for selecting fields of GithubUser entities.
type GithubUserSelect struct {
	*GithubUserQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gus *GithubUserSelect) Aggregate(fns ...AggregateFunc) *GithubUserSelect {
	gus.fns = append(gus.fns, fns...)
	return gus
}

// Scan applies the selector query and scans the result into the given value.
func (gus *GithubUserSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gus.ctx, "Select")
	if err := gus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GithubUserQuery, *GithubUserSelect](ctx, gus.GithubUserQuery, gus, gus.inters, v)
}

func (gus *GithubUserSelect) sqlScan(ctx context.Context, root *GithubUserQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gus.fns))
	for _, fn := range gus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
